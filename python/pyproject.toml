[project]
name = "sciailab-research-runtime"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.127.0",
  "uvicorn>=0.40.0"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
